import 'package:flutter/cupertino.dart';

class ConnectAirtableModal extends StatelessWidget {
  final dynamic userActions;

  const ConnectAirtableModal({Key key, this.userActions}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
