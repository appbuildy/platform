import 'package:flutter/material.dart';

class ToolboxShapeComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 35,
      height: 35,
      color: Colors.white,
    );
  }
}
