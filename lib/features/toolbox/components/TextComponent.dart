import 'package:flutter/material.dart';

class ToolboxTextComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('Text', style: TextStyle(fontSize: 16.0),);
  }
}
